# Calculator
​A lightweight, sequential CLI Calculator built in Java that parses and evaluates complex mathematical expressions dynamically from a single string input.It processes arithmetic expressions sequentially (e.g., 10+5*2=) by extracting tokens on the fly using dual-phase loop logic
